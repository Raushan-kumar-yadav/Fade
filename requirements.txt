# Fade Python dependencies
# Auto-installed by scripts/killports.js on every `npm run dev`
# Full install: pip install -r requirements.txt

# ── Core backend ──────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
av>=13.0
Pillow>=10.0
numpy>=1.26
python-multipart>=0.0.9
psutil>=6.0.0

# ── AI Agent (LangGraph) ──────────────────────────────────────────────────────
langgraph>=0.2
langchain-core>=0.3
httpx>=0.27
python-dotenv>=1.0

# ── LLM Providers (uncomment what you use) ───────────────────────────────────
langchain-anthropic>=0.3      # FADE_AI_PROVIDER=claude
langchain-openai>=0.2         # FADE_AI_PROVIDER=openai | tabi  ← ACTIVE
# langchain-google-genai>=2.0 # FADE_AI_PROVIDER=gemini
# langchain-groq>=0.2         # FADE_AI_PROVIDER=groq
# langchain-ollama>=0.2       # FADE_AI_PROVIDER=ollama

# ── Whisper (optional, for transcription) ────────────────────────────────────
# openai-whisper>=20231117
